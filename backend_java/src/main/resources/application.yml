spring:
  datasource:
    url: jdbc:mysql://106.12.52.116:1999/freetrader?useSSL=true&serverTimezone=Asia/Shanghai&characterEncoding=utf-8
    username: free
    password: free123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none # 生产环境禁用自动建表
    show-sql: false # 生产环境不打印 SQL

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    enabled: true

# JWT配置
jwt:
  secret: your-256-bit-secret-key-change-this-in-production-environment
  expiration: 86400000 # 24小时，单位毫秒

mybatis-plus:
  type-aliases-package: com.ives.api.model.entity
  # XML 映射文件路径（如果需要自定义 XML）
  mapper-locations: classpath:mapper/**/*.xml