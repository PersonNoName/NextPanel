<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.personnoname.nextpanelapi.mapper.UserInfoMapper">
    <select id="selectByUsername" resultType="UserInfo">
        SELECT * FROM user_info WHERE username = #{username} AND deleted_at IS NULL LIMIT 1
    </select>

    <select id="selectByEmail" resultType="UserInfo">
        SELECT * FROM user_info
        WHERE email = #{email} AND deleted_at IS NULL
        LIMIT 1
    </select>

    <select id="existsByUsername" resultType="java.lang.Boolean">
        SELECT COUNT(1) FROM user_info
        WHERE username = #{username} AND deleted_at IS NULL
    </select>

    <select id="existsByEmail" resultType="java.lang.Boolean">
        SELECT COUNT(1) FROM user_info
        WHERE email = #{email} AND deleted_at IS NULL
    </select>
</mapper>